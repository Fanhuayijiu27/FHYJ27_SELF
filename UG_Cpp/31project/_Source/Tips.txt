AI辅助开发新项目重要提示：

1、当前项目的所有 .h 和 .cpp 文件都是 GBK（GB2312）编码。在读取、搜索和修改文件时，绝对禁止使用会破坏 GBK 编码的工具（如 Edit 工具、sed、awk 或默认的 UTF-8 读写脚本）。请务必使用支持显式指定 GBK 编码的方式（Python 指定 encoding='gbk'）来进行替换操作！

2、自动编译桥：
为了提高开发效率，我已经在这个项目中为你搭建好了一套名为“自动编译桥”的跨系统自动化编译系统。
从现在开始，你拥有了完整的【闭环开发】能力。请仔细阅读并严格遵守以下开发规范：
### 1. 我们的运行环境
- 我们在物理机（Host）的主目录 `D:\VMwareSHARE\HRproject\31project\` 下编写代码。
- 实际的编译环境是一台老旧的 Windows 7 虚拟机（包含 VS2015 和 NX12 环境）。
- 你不需要关心虚拟机内部的细节，因为我已经为你做好了自动化接口。
### 2. 一键编译命令
每当你修改完 C++ 源码，需要测试是否能通过编译时，请**主动调用终端执行以下命令**触发编译流程：
`powershell -ExecutionPolicy Bypass -File D:\VMwareSHARE\HRproject\31project\tools_autoVS2015\request_vm_build.ps1`
### 3. 全自动闭环工作流
在未来的所有任务中，请你采用以下工作流，不要再让我手动复制报错：
1.**指向文件**：确认是否指向的是（31project\_Source\FeatureSpy260722\FeatureSpy260722）项目文件夹。
2. **修改代码**：根据我的需求，修改 `.cpp` 或 `.h` 文件。
3. **触发编译**：代码修改完成后，你必须主动在终端里运行上述的 `.ps1` 编译命令。
4. **读取结果**：该脚本会在终端阻塞等待，并最终在终端标准输出（stdout）中打印出完整的 MSBuild 编译日志。
5. **自我修复**：
   - 如果终端输出 `Result: BUILD_SUCCESS` 且 `0 个错误`，请告诉我编译通过，任务完成。
   - 如果遇到 `Result: BUILD_FAILED` 或包含任何 C++ 语法报错（如 `C2065` 等），**请你仔细阅读终端里的错误所在行数，先把第一个报错进行修复（C++开发时把第一个报错进行修复后面的错误可能就顺便解决了），自动修改代码，然后再次执行编译命令**。
6. **循环迭代**：不断重复“改代码 -> 跑命令 -> 看日志”的步骤，直到完全没有错误，注意实时向我汇报当前运行结果。
你听明白你的新工作流了吗？如果听明白了，请回复“我已经掌握自动编译桥机制，随时准备开始自动循环开发。”

3、在调试代码的时候，可以使用代码  uc1601()  进行显示对话框，来确定代码运行到哪里了

4、新项目基于旧项目开发，以模仿旧项目、简洁易维护、易阅读为宗旨，
（1）能用旧代码已有的函数进行实现功能就用旧代码，尤其是基础辅助函数！
（2）使用_31 后缀，_31 只加在需要明显区分的新结构体、新函数、新成员函数名称后面；TopFaceVector、InWallVector 等普通成员变量继续沿用旧命名。
（3）非必要尽量避免新建新的 .cpp 或 .h 文件
（4）定义函数、变量等过程要模仿旧代码，达到代码一致性