# FeatureSpy260721 修改日志

## 概述

- **项目名称**: FeatureSpy260721（特征识别模块）
- **创建日期**: 2026-07-21
- **基础代码**: FeatureSpy (NX11, fxy, 2024-01-03)
- **开发环境**: NX 12 + VS2015 (v140) + x64
- **创建者**: ug12

---

## 一、文件变更清单

### 1.1 新增文件（从旧项目复制）

| 文件 | 来源 | 行数 | 说明 |
|------|------|------|------|
| `SpyFunctions.cpp` | `FBM_Win32_NX11_NUAA_finalversion/FBM/FeatureSpy/` | 4,812 | 核心特征识别算法（凸台/槽/孔/筋/轮廓） |
| `SpyFunctions.h` | 同上 | 169 | 特征识别函数声明（30+ DllExport函数） |
| `xml.cpp` | 同上 | 7,418 | XML输出实现（COM/IXMLDOMDocument） |
| `xml.h` | 同上 | 106 | XML类声明 |
| `HuNXOpen.h` | 同上 | 613 | NXOpen C++ API头文件聚合 |
| `uf_all.h` | 同上 | 56 | UF API头文件聚合 |

### 1.2 已存在的支撑文件（未修改）

| 文件 | 位置 | 说明 |
|------|------|------|
| `PublicFunctions.cpp/h` | `..\..\UserMachiningPublicModule\` | 几何工具函数（3,810行） |
| `FeaturePublicClass.h` | 同上 | 30+特征数据结构体定义 |
| `ProcessPublicClass.h` | 同上 | 工艺参数结构体 |

### 1.3 重写的文件

| 文件 | 基础来源 | 修改内容 |
|------|----------|----------|
| `FeatureSpy260721.hpp` | 旧 FeatureSpy.hpp | 类名重命名、UI控件调整、移除搜索功能声明 |
| `FeatureSpy260721.cpp` | 旧 FeatureSpy.cpp (12,695行) | 类名全局替换、删除3个不需要的控件、约-264行 |
| `FeatureSpy260721.vcxproj` | FeatureSpy260714.vcxproj | 项目名/源文件/include路径更新 |
| `FeatureSpy260721.sln` | FeatureSpy260714.sln | 新建VS2015解决方案 |

### 1.4 更新的配置文件

| 文件 | 修改内容 |
|------|----------|
| `..\..\..\Startup\Hu.men` | 菜单ACTION改为 FeatureSpy260721.dll |
| `..\..\..\tools_autoVS2015\request_vm_build.ps1` | 同步路径改为 FeatureSpy260721 |
| `..\..\..\tools_autoVS2015\vm_build_watcher.bat` | 编译路径改为 FeatureSpy260721 |

---

## 二、代码修改详情

### 2.1 FeatureSpy260721.hpp

**类名更改**: `FeatureSpy` → `FeatureSpy260721`
**Guard宏更改**: `FEATURESPY_H_INCLUDED` → `FEATURESPY260721_H_INCLUDED`
**DLX文件名**: `FeatureSpy.dlx` → `FeatureSpy260721.dlx`
**NX版本**: NX 11 → NX 12

**移除的成员变量**（2个）:
- `Face * pSelectedFace2;` — 仅用于 face_select01 搜索功能
- `Face * pSelectedFace3;` — 仅用于 button03 搜索功能

**移除的成员函数声明**（1个）:
- `void FeatureSearchResultDisplay(...)` — 搜索结果列表显示（依赖 list_box01）

**移除的成员变量**（1个）:
- `std::vector<NXString> FeatureSearchResultDisplayStringTitle;` — 搜索结果标题

**Private UI控件调整**:
| 旧控件名 | 新控件名 | 操作 |
|----------|----------|------|
| `group0` | `group` | 重命名（匹配新DLX布局） |
| `group` | `group1` | 重命名 |
| `group1` | `group2` | 重命名 |
| `group2` | `group3` | 重命名 |
| `face_select01` | — | **删除** |
| `button03` | — | **删除** |
| `list_box01` | — | **删除** |
| `bodySelect0` | 保留 | 不变 |
| `face_select0` | 保留 | 不变（手动分型面选择） |
| `enum0` | 保留 | 不变 |
| `button0` | 保留 | 不变 |
| `list_box0` | 保留 | 不变 |
| `button01` | 保留 | 不变 |
| `enum01` | 保留 | 不变 |
| `button02` | 保留 | 不变 |

**保留的全部功能**:
- 所有 ~60+ 个颜色设置函数声明（SetAllBossColor, SetAllHoleColor 等）
- 所有特征容器声明（Feature_NormalBoss, SealFace_* 等 50+ 个 vector）
- 所有数据成员（pSelectedBody, Begin_Face, _Approach_Normal 等）
- 树列表相关成员（FeatureSpyResultDisplayStringTitle, GrooveClassflyList 等）
- GetInfomation(), FeatureSpyResultDisplay(), ResponseToFeatureList(), CreateXmlFile() 等函数声明

### 2.2 FeatureSpy260721.cpp

**全局替换**（4项）:
1. `FeatureSpy::` → `FeatureSpy260721::`（75处）
2. `FeatureSpy *` → `FeatureSpy260721 *`（指针类型声明）
3. `"FeatureSpy.dlx"` → `"FeatureSpy260721.dlx"`
4. `"FeatureSpy.hpp"` → `"FeatureSpy260721.hpp"`

**文件头更新**:
- 创建者: fxy → ug12
- 版本: NX 11 → NX 12
- 日期: 01-03-2024 → 07-21-2026

**initialize_cb() 修改**:
- `FindBlock("group0")` → `FindBlock("group")`（匹配新DLX控件名）
- `FindBlock("group")` → `FindBlock("group1")`
- `FindBlock("group1")` → `FindBlock("group2")`
- `FindBlock("group2")` → `FindBlock("group3")`
- 删除: `FindBlock("face_select01")`
- 删除: `FindBlock("button03")`
- 删除: `FindBlock("list_box01")`

**dialogShown_cb() 修改**:
- 删除: `face_select01->SetFaceRules(1);`
- 保留: `face_select0->SetFaceRules(1);` 和提示信息

**update_cb() 删除的控件处理块**（共约238行）:
- `else if(block == face_select01)` 处理块（~30行）— 手动面搜索逻辑
- `else if(block == button03)` 处理块（~200行）— 按面搜索特征 + FeatureSearchResultDisplay调用
- `else if(block == list_box01)` 处理块（~4行）— 空stub

**删除的函数实现**（约22行）:
- `FeatureSpy260721::FeatureSearchResultDisplay()` — 整个函数体

**完全保留的功能**:
- `ufusr()` 入口（含 UF_initialize/UF_terminate）
- `bodySelect0` 处理逻辑 — 获取工作部件、提取所有面、激活面选择
- `face_select0` 处理逻辑 — 手动选择分型面、计算法向量
- `enum0` 处理 — 空stub
- `button0` 处理逻辑 — GetInfomation() + 全部6类特征识别 + XML生成
- `list_box0` 处理逻辑 — GetSelectedIndexAndFeatureName + ResponseToFeatureList
- `button01` 处理逻辑 — CreateXmlFile()
- `enum01` 处理逻辑 — 空stub
- `button02` 处理逻辑 — 特征删除（凸台/槽/孔/筋/轮廓全部类型）
- `GetDefaultColor()` — UF_OBJ_ask_display_properties
- `GetInfomation()` — 自动检测顶面/底面/平行面
- `FeatureSpyResultDisplay()` — 构建树列表显示
- `ResponseToFeatureList()` — ~7,400行树展开/折叠逻辑
- `CreateXmlFile()` — MFC文件对话框 + XML保存
- 全部 20+ `Set*Color()` 函数

### 2.3 FeatureSpy260721.vcxproj

**移除**: Win32配置（仅保留x64）
**配置类型**: Application → DynamicLibrary
**新增**: `<UseOfMfc>Dynamic</UseOfMfc>`（CreateXmlFile使用MFC CFileDialog）
**新增**: `<OutDir>..\..\..\Application\</OutDir>`
**Include路径**:
```
..\..\UserMachiningPublicModule;$(UGII_BASE_DIR)\ugopenpp;$(UGII_BASE_DIR)\ugopen
```
**源文件新增**:
```xml
<ClCompile Include="SpyFunctions.cpp" />
<ClCompile Include="xml.cpp" />
```
**头文件新增**:
```xml
<ClInclude Include="HuNXOpen.h" />
<ClInclude Include="SpyFunctions.h" />
<ClInclude Include="uf_all.h" />
<ClInclude Include="xml.h" />
```
**输出文件**: `FeatureSpy260721.dll` / `FeatureSpy260721.lib`
**链接库**: 完整NX12依赖库列表（60+ lib）

---

## 三、控件对照表

| 新 FeatureSpy260721 | 旧 FeatureSpy | 功能 | 状态 |
|---------------------|---------------|------|------|
| `bodySelect0` | `bodySelect0` | 选择实体模型 | ✅ 逻辑完全一致 |
| `face_select0` | `face_select0` | 手动选择分型面 | ✅ 逻辑完全一致 |
| `enum0` | `enum0` | 特征类型下拉 | ✅ 处理框架保留 |
| `button0` | `button0` | 特征识别按钮 | ✅ 逻辑完全一致 |
| `list_box0` | `list_box0` | 特征树显示 | ✅ 逻辑完全一致 |
| `button01` | `button01` | 导出XML | ✅ 逻辑完全一致 |
| `enum01` | `enum01` | 删除上下文（下拉） | ✅ 逻辑完全一致 |
| `button02` | `button02` | 删除此特征 | ✅ 逻辑完全一致 |
| — | ~~face_select01~~ | 搜索面选择 | ❌ 已删除 |
| — | ~~button03~~ | 搜索特征 | ❌ 已删除 |
| — | ~~list_box01~~ | 搜索结果列表 | ❌ 已删除 |

---

## 四、⚠️ 需要后续确认的事项

### 4.1 enum0 枚举字符串

当前代码中 `button0` 和 `button02` 处理函数使用的字符串比较值仍为旧版：

| 位置 | 旧字符串 | 需确认的新DLX值 |
|------|----------|-----------------|
| button0 ~410行 | `"全部特征"` | 是否需要？ |
| button0 ~598行 | `"凸台类特征"` | `"凸台"` |
| button0 ~614行 | `"槽类特征"` | `"槽（下陷）"` |
| button0 ~663行 | `"孔类特征"` | `"孔"` |
| button0 ~781行 | `"筋类特征"` | 是否需要？ |
| button0 ~850行 | `"轮廓类特征"` | 是否需要？ |

**说明**: 这些字符串必须与 `FeatureSpy260721.dlx` 中 enum0 的枚举值完全一致。请根据DLX文件中的实际值更新。

### 4.2 编译验证

编译需要在虚拟机（Win7 + VS2015）环境中进行：
1. 按 `Ctrl+Shift+B` 触发 build_bridge 同步和编译
2. 检查 `build_bridge_autoVS2015/build_latest.log` 查看编译结果
3. 在NX12中加载编译好的 `FeatureSpy260721.dll`

---

## 五、改动统计

| 类别 | 数量 |
|------|------|
| 新增文件 | 7个（含.sln） |
| 重写文件 | 3个（.hpp/.cpp/.vcxproj） |
| 修改配置文件 | 3个（Hu.men/ps1/bat） |
| 删除的UI控件 | 3个（face_select01/button03/list_box01） |
| 删除的成员变量 | 2个（pSelectedFace2/pSelectedFace3） |
| 删除的成员函数 | 1个（FeatureSearchResultDisplay） |
| 总代码行数 | ~12,431行（主.cpp）+ 12,230行（支撑.cpp） |
